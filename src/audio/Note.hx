package audio;

enum Note
{
    A_F(octave:Int);
    A(octave:Int);
    A_S(octave:Int);

    B_F(octave:Int);
    B(octave:Int);
    B_S(octave:Int);

    C_F(octave:Int);
    C(octave:Int);
    C_S(octave:Int);

    D_F(octave:Int);
    D(octave:Int);
    D_S(octave:Int);

    E_F(octave:Int);
    E(octave:Int);
    E_S(octave:Int);

    F_F(octave:Int);
    F(octave:Int);
    F_S(octave:Int);

    G_F(octave:Int);
    G(octave:Int);
    G_S(octave:Int);
}
